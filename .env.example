# Headline_News 环境变量模板
# 复制为 .env 后按需修改；.env 本身不要提交到 git（已在 .gitignore 中排除）
# 本地直接运行可全部留空，将自动使用下方默认值。

# --- MySQL ---
DB_USER=root
DB_PASSWORD=123456
DB_HOST=localhost
DB_PORT=3306
DB_NAME=news_app

# --- Redis ---
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_DB=0
# 无密码时留空或删掉该行
REDIS_PASSWORD=
